# 📚 EduSchedule - Система управления расписанием

## 📋 Описание проекта

**EduSchedule** — современная информационная система для управления расписанием учебного заведения. Система позволяет эффективно управлять учебными группами, преподавателями, дисциплинами, аудиториями и формировать расписание занятий.

### ✨ Основные возможности

- 👥 Управление учебными группами
- 👨‍🏫 Управление преподавателями  
- 📖 Управление дисциплинами
- 🏫 Управление аудиториями
- 📅 Создание и редактирование расписания
- 🔍 Фильтрация расписания по группам и дням недели
- 💎 Современный и интуитивный интерфейс

## 🛠 Технологический стек

### Backend
- **Node.js** - серверная платформа
- **Express.js** - веб-фреймворк
- **SQLite** - база данных
- **body-parser** - парсинг HTTP запросов
- **cors** - настройка CORS политик

### Frontend  
- **HTML5** - разметка
- **CSS3** - стилизация (современный дизайн)
- **JavaScript (ES6+)** - интерактивность
- **Google Fonts (Inter)** - типографика

## 📁 Структура проекта

```
edu-scheduler/
├── public/              # Статические файлы frontend
│   ├── index.html      # Главная страница
│   ├── styles.css      # Стили приложения
│   └── app.js          # Клиентская логика
├── src/
│   └── database/
│       └── init-db.js  # Скрипт инициализации БД
├── database.db         # Файл базы данных SQLite
├── server.js           # Основной серверный файл
├── package.json        # Конфигурация проекта
├── TECH_TZ.md         # Техническое задание
└── README.md          # Документация
```

## 🗃 Структура базы данных

### Таблица `groups` (Учебные группы)
- `id` - уникальный идентификатор
- `name` - название группы

### Таблица `teachers` (Преподаватели)
- `id` - уникальный идентификатор
- `full_name` - ФИО преподавателя

### Таблица `subjects` (Дисциплины)
- `id` - уникальный идентификатор
- `name` - название дисциплины

### Таблица `classrooms` (Аудитории)
- `id` - уникальный идентификатор
- `room_number` - номер аудитории

### Таблица `schedule` (Расписание)
- `id` - уникальный идентификатор
- `group_id` - ссылка на группу
- `subject_id` - ссылка на дисциплину
- `teacher_id` - ссылка на преподавателя
- `classroom_id` - ссылка на аудиторию
- `day_of_week` - день недели
- `lesson_number` - номер пары (1-8)

## 🚀 Установка и запуск

### Требования
- Node.js (версия 14 или выше)
- npm (обычно идёт вместе с Node.js)

### Шаги установки

1. **Клонируйте репозиторий или скачайте проект**
   ```bash
   cd edu-scheduler
   ```

2. **Установите зависимости**
   ```bash
   npm install
   ```

3. **Инициализируйте базу данных**
   ```bash
   npm run init-db
   ```

4. **Запустите сервер**
   ```bash
   npm start
   ```
   
   Для режима разработки с автоперезагрузкой:
   ```bash
   npm run dev
   ```

5. **Откройте приложение в браузере**
   ```
   http://localhost:3000
   ```

## 📱 Использование

### Навигация
Используйте боковое меню для переключения между разделами:
- **Расписание** - просмотр и управление расписанием
- **Группы** - управление учебными группами
- **Преподаватели** - управление преподавателями
- **Дисциплины** - управление дисциплинами  
- **Аудитории** - управление аудиториями

### Работа с данными
1. **Добавление** - нажмите кнопку "Добавить" в соответствующем разделе
2. **Редактирование** - нажмите кнопку "Изменить" рядом с записью
3. **Удаление** - нажмите кнопку "Удалить" рядом с записью
4. **Фильтрация** - используйте выпадающие списки для фильтрации расписания

## 🔌 API Endpoints

### Группы
- `GET /api/groups` - получить все группы
- `GET /api/groups/:id` - получить группу по ID
- `POST /api/groups` - создать новую группу
- `PUT /api/groups/:id` - обновить группу
- `DELETE /api/groups/:id` - удалить группу

### Преподаватели
- `GET /api/teachers` - получить всех преподавателей
- `GET /api/teachers/:id` - получить преподавателя по ID
- `POST /api/teachers` - создать нового преподавателя
- `PUT /api/teachers/:id` - обновить преподавателя
- `DELETE /api/teachers/:id` - удалить преподавателя

### Дисциплины
- `GET /api/subjects` - получить все дисциплины
- `GET /api/subjects/:id` - получить дисциплину по ID
- `POST /api/subjects` - создать новую дисциплину
- `PUT /api/subjects/:id` - обновить дисциплину
- `DELETE /api/subjects/:id` - удалить дисциплину

### Аудитории
- `GET /api/classrooms` - получить все аудитории
- `GET /api/classrooms/:id` - получить аудиторию по ID
- `POST /api/classrooms` - создать новую аудиторию
- `PUT /api/classrooms/:id` - обновить аудиторию
- `DELETE /api/classrooms/:id` - удалить аудиторию

### Расписание
- `GET /api/schedule` - получить расписание (с фильтрами)
- `POST /api/schedule` - создать новое занятие
- `PUT /api/schedule/:id` - обновить занятие
- `DELETE /api/schedule/:id` - удалить занятие

## 🎨 Особенности дизайна

- **Минималистичный интерфейс** - чистый и современный дизайн
- **Адаптивная вёрстка** - корректное отображение на разных устройствах
- **Интуитивная навигация** - простое и понятное управление
- **Визуальная обратная связь** - анимации и hover-эффекты
- **Цветовая схема** - профессиональные цвета с акцентом на удобство

## 🔒 Ограничения системы

- Одно занятие не может проводиться в одно время для:
  - Одной группы
  - Одного преподавателя  
  - Одной аудитории
- Номер пары от 1 до 8
- Дни недели: Понедельник - Суббота

## 📈 Возможности расширения

- 🔐 Добавление системы авторизации
- 👤 Разграничение ролей пользователей
- 📊 Экспорт расписания в различные форматы
- 📱 Мобильное приложение
- 🔄 Синхронизация с внешними системами
- 📧 Уведомления об изменениях в расписании

## 🤝 Контакты

Проект разработан в рамках дисциплины "Системы управления базами данных"

---

*© 2024 EduSchedule - Система управления расписанием учебного заведения*
