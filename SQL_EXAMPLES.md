 # Примеры SQL-запросов для проекта EduSchedule

Этот файл содержит примеры базовых SQL-запросов для каждой таблицы в базе данных. Они могут быть использованы для тестирования, отладки или прямого взаимодействия с файлом `database.db` через любой SQLite-клиент.

---

### 📚 Таблица `groups` (Учебные группы)

**1. Получить все группы**
```sql
SELECT * FROM groups ORDER BY name;
```

**2. Добавить новую группу**
```sql
INSERT INTO groups (name) VALUES ('ПИН(б)-1-20');
```

**3. Обновить название группы**
```sql
UPDATE groups SET name = 'ПИН(б)-1-21' WHERE id = 4;
```

**4. Удалить группу**
```sql
DELETE FROM groups WHERE id = 4;
```

---

### 👨‍🏫 Таблица `teachers` (Преподаватели)

**1. Получить всех преподавателей**
```sql
SELECT * FROM teachers ORDER BY full_name;
```

**2. Добавить нового преподавателя**
```sql
INSERT INTO teachers (full_name) VALUES ('Абдуллаев Рустам Дамирович');
```

**3. Обновить ФИО преподавателя**
```sql
UPDATE teachers SET full_name = 'Абдуллаева Рушана Дамировна' WHERE id = 9;
```

**4. Удалить преподавателя**
```sql
DELETE FROM teachers WHERE id = 9;
```

---

### 📖 Таблица `subjects` (Дисциплины)

**1. Получить все дисциплины**
```sql
SELECT * FROM subjects ORDER BY name;
```

**2. Добавить новую дисциплину**
```sql
INSERT INTO subjects (name) VALUES ('Философия');
```

---

### 🚪 Таблица `classrooms` (Аудитории)

**1. Получить все аудитории**
```sql
SELECT * FROM classrooms ORDER BY room_number;
```

**2. Добавить новую аудиторию**
```sql
INSERT INTO classrooms (room_number) VALUES ('3/111');
```

---

### 🗓️ Таблица `schedule` (Расписание)

**1. Получить все записи расписания для конкретной группы**
```sql
SELECT 
    s.id, 
    s.day_of_week, 
    s.lesson_number, 
    g.name as group_name, 
    sub.name as subject_name, 
    t.full_name as teacher_name, 
    c.room_number
FROM schedule s
JOIN groups g ON s.group_id = g.id
JOIN subjects sub ON s.subject_id = sub.id
JOIN teachers t ON s.teacher_id = t.id
JOIN classrooms c ON s.classroom_id = c.id
WHERE g.name = 'ПИН(дот)-2-23'
ORDER BY s.day_of_week, s.lesson_number;
```

**2. Добавить новое занятие в расписание**
*Сначала нужно получить ID группы, преподавателя, дисциплины и аудитории.*
```sql
-- Предположим, мы получили: group_id=1, subject_id=3, teacher_id=1, classroom_id=1
INSERT INTO schedule (group_id, subject_id, teacher_id, classroom_id, day_of_week, lesson_number)
VALUES (1, 3, 1, 1, 'Четверг', 4);
```

**3. Обновить занятие (например, сменить аудиторию)**
```sql
-- Предположим, ID записи расписания = 7, новый ID аудитории = 2
UPDATE schedule SET classroom_id = 2 WHERE id = 7;
```

**4. Удалить занятие из расписания**
```sql
DELETE FROM schedule WHERE id = 7;
```
